# Интерактивна Периодична Таблица - Skeleton файлове

## 📚 Съдържание

Този пакет съдържа скелетни файлове за проекта "Интерактивна Периодична Таблица", които са почти завършени и са готови за използване с минимални добавки.

### Файлове:

1. **periodic-table-skeleton.html** - HTML структура 
2. **periodic-table-skeleton.css** - CSS стилове 
3. **periodic-table-skeleton.js** - JavaScript логика 

## 🎯 Как да използвате файловете

### Стъпка 1: Преименувайте файловете

Преименувайте файловете, като махнете `-skeleton` от средата:

```
periodic-table-skeleton.html  →  periodic-table.html
periodic-table-skeleton.css   →  periodic-table.css
periodic-table-skeleton.js    →  periodic-table.js
```

### Стъпка 2: Отворете във Visual Studio Code

1. Отворете Visual Studio Code
2. File → Open Folder
3. Изберете папката с файловете
4. Отворете всички 3 файла в различни табове

### Стъпка 3: Проверете какво има

Отворете `periodic-table.html` в браузър:
- Виждате ли заглавието?
- Има ли контролен панел (филтър и търсене)?
- Виждате ли празната таблица?
- Виждат ли се секциите за детайли и викторина?

**Забележка:** Таблицата е празна защото елементите се генерират с JavaScript!

### Стъпка 4: Следвайте TODO инструкциите

В файловете има TODO коментари. Работете по седмици:

- **Седмици 1-2**: Попълнете HTML 
- **Седмици 3-4**: Попълнете CSS 
- **Седмици 5-10**: Попълнете JavaScript функциите

## 💡 Особености на този проект

### HTML файл (Много TODO)
- Свързване на CSS и JS файлове
- Създаване на header секция
- Контролен панел (филтър, търсене, статистики)
- Div за периодична таблица
- Детайли за елемент (много полета)
- Викторина секция (сложна структура)
- Легенда с цветове
- **Общо ~7 големи TODO секции**

### CSS файл (Пълен skeleton с TODO)
- CSS променливи в :root
- Reset и body стилове
- Header и контроли
- CSS Grid за таблицата
- Стилове за елементи
- Цветово кодиране за 6 категории
- Детайли и викторина стилове
- Responsive media queries
- **Общо ~60+ TODO задачи**

### JavaScript файл (Много TODO задачи)
- Създаване на масив с 25+ елемента
- Глобални променливи и DOM връзки
- For цикли за рендериране
- If-else за филтриране и търсене
- Switch за категории
- Event listeners
- Викторина логика
- Fisher-Yates shuffle алгоритъм
- **Общо ~30+ TODO задачи**

## 📖 Проверка на прогреса

### Седмици 1-2 (HTML):
- ✅ Свързали ли сте CSS файла в head?
- ✅ Създали ли сте header с h1 и subtitle?
- ✅ Има ли пълен controls панел (филтър, търсене, stats)?
- ✅ Създали ли сте div с id="periodicTable"?
- ✅ Има ли пълна element-details секция със 6 полета?
- ✅ Създали ли сте quiz-section с всички подсекции?
- ✅ Има ли legend със 6 категории?
- ✅ Свързали ли сте JavaScript файла преди </body>?

### Седмици 3-4 (CSS):
- ✅ Създали ли сте :root с всички променливи?
- ✅ Има ли reset стилове (*)?
- ✅ Body стиловете са зададени?
- ✅ Header има градиент фон и сенки?
- ✅ Controls панелът е flexbox?
- ✅ Periodic table използва CSS Grid с 18 колони?
- ✅ Елементите имат правилни размери и padding?
- ✅ Всички 6 категории имат свои цветове?
- ✅ Hover ефектите работят?
- ✅ Има ли 3 media queries за responsive?

### Седмици 5-6 (JavaScript - Масиви и рендериране):
- ✅ Създали ли сте масив `elements` с поне 25 елемента?
- ✅ Всеки елемент има ли всички 10 свойства (number, symbol, name, mass, category, period, group, row, col, description)?
- ✅ Създали ли сте 6 глобални променливи (currentFilter, searchTerm, и т.н.)?
- ✅ Свързали ли сте всички DOM елементи с getElementById?
- ✅ Функцията `renderPeriodicTable()` използва ли for цикъл?
- ✅ Създавате ли div елементи динамично с createElement?
- ✅ Задавате ли gridRow и gridColumn за всеки елемент?
- ✅ Добавяте ли dataset атрибути?
- ✅ Показват ли се елементите в таблицата?
- ✅ Има ли click event listener на всеки елемент?

### Седмици 7-8 (Показване на детайли, Филтриране и търсене):
- ✅ Функцията `showElementDetails()` попълва ли всички полета?
- ✅ Използвате ли switch statement за превод на категорията?
- ✅ Scroll-ва ли до детайлите при клик?
- ✅ Функцията `filterElements()` взима ли всички елементи с querySelectorAll?
- ✅ Използвате ли for цикъл за обхождане?
- ✅ Има ли if-else проверка за categoryMatch?
- ✅ Има ли if-else проверка за searchMatch?
- ✅ Добавяте/махате ли класа 'hidden' правилно?
- ✅ Обновява ли се броячът на видими елементи?
- ✅ Event listeners за филтър и търсене работят ли?

### Седмици 9-10 (Викторина):
- ✅ `startQuiz()` нулира ли променливите правилно?
- ✅ `getRandomElements()` използва ли Fisher-Yates алгоритъм?
- ✅ Функцията връща ли 10 случайни елемента?
- ✅ `showQuizQuestion()` проверява ли дали викторината е завършена?
- ✅ Генерират ли се 2 типа въпроси (symbol и name)?
- ✅ `generateSymbolOptions()` създава ли 4 опции?
- ✅ `generateNameOptions()` работи ли аналогично?
- ✅ `shuffleArray()` разбърква ли опциите?
- ✅ `renderQuizOptions()` използва ли for цикъл?
- ✅ Всяка опция има ли click event listener?
- ✅ `checkAnswer()` блокира ли опциите след избор?
- ✅ Има ли if-else за правилен/грешен отговор?
- ✅ Оцветяват ли се опциите правилно?
- ✅ Обновява ли се quizScore правилно?
- ✅ След 2 секунди показва ли се следващ въпрос?
- ✅ `showQuizResults()` изчислява ли процент?
- ✅ Използва ли if-else за различни съобщения?
- ✅ Показва ли се бутон за нова викторина?

### Седмица 11 (Тестване):
- ✅ Всички функции работят без грешки?
- ✅ Няма грешки в Console (F12)?
- ✅ Responsive дизайнът работи на телефон?
- ✅ Кодът е коментиран и разбираем?

## 🆘 Често срещани проблеми

### Проблем: "Elements is not defined"
**Решение:** Уверете се, че сте създали масива `elements` в началото на JS файла.

### Проблем: Елементите не се показват
**Решение:** 
1. Проверете дали `for` цикълът в `renderElements()` е написан правилно
2. Проверете дали добавяте елементите към `periodicTable` div-а
3. Погледнете Console за грешки

### Проблем: Филтрирането не работи
**Решение:**
1. Проверете дали event listener-ът за `filterType` е добавен
2. Уверете се че switch statement покрива всички категории
3. Проверете дали добавяте/махате класа `hidden` правилно

### Проблем: Търсенето не работи
**Решение:**
1. Проверете дали event listener-ът за `searchInput` е добавен
2. Уверете се че `.toLowerCase()` е използван за case-insensitive търсене
3. Проверете дали търсенето е както по име, така и по символ

### Проблем: Викторината не стартира
**Решение:**
1. Проверете дали event listener-ът за `startQuiz` бутона е добавен
2. Уверете се че функцията `startQuiz()` показва quiz секцията
3. Проверете дали `generateQuizQuestion()` генерира правилно въпроси

### Проблем: CSS стиловете липсват
**Решение:**
1. Проверете дали `<link>` таг-ът в HTML е правилен
2. Уверете се че CSS файлът е в същата папка като HTML
3. Refresh-нете браузъра с Ctrl+Shift+R (hard refresh)

## 💻 Структура на JavaScript кода

### Основни променливи
```javascript
const elements = [/* масив с елементи */];
let currentQuizQuestion = 0;
let quizScore = 0;
let usedElements = [];
```

### Ключови функции

**renderElements(elementsToRender)**
- Рендерира елементите в таблицата
- Използва for цикъл
- Създава HTML за всеки елемент

**filterElements(category)**
- Филтрира елементите по категория
- Използва switch statement
- Показва/скрива елементи чрез класа `hidden`

**searchElements(query)**
- Търси елементи по име или символ
- Използва if-else и .includes()
- Case-insensitive търсене

**showElementDetails(element)**
- Показва детайлна информация за елемент
- Попълва всички полета в секцията за детайли

**startQuiz()**
- Инициализира викторината
- Нулира точките и въпросите
- Генерира първия въпрос

**generateQuizQuestion()**
- Избира случаен елемент
- Генерира 4 възможни отговора
- Показва въпроса на екрана

**checkQuizAnswer(selected, correct)**
- Проверява дали отговорът е правилен
- Обновява точките
- Показва следващ въпрос или резултат

## 🎨 CSS персонализация

### Променливи за цветове

В началото на CSS файла има `:root` секция с променливи:

```css
:root {
    --bg-dark: #0a0e27;
    --bg-card: #1a1f3a;
    --accent-blue: #4f6aff;
    --accent-purple: #8b5cf6;
    /* и др. */
}
```

Можете да промените тези стойности за да персонализирате цветовете!

### Цветове на категориите

```css
:root {
    --nonmetal: #ff6b9d;    /* Розово за неметали */
    --metal: #fbbf24;       /* Жълто за метали */
    --metalloid: #a78bfa;   /* Лилаво за металоиди */
    --noble-gas: #60a5fa;   /* Синьо за благородни газове */
    --alkali: #f97316;      /* Оранжево за алкални метали */
    --alkaline: #84cc16;    /* Зелено за алкалоземни */
}
```

## 📚 Примерна структура на масив с елементи

```javascript
const elements = [
    {
        number: 1,
        symbol: 'H',
        name: 'Водород',
        mass: '1.008',
        category: 'nonmetal',
        period: 1,
        group: 1,
        description: 'Най-лекият и най-често срещан елемент във Вселената.',
        position: { row: 1, col: 1 }
    },
    {
        number: 2,
        symbol: 'He',
        name: 'Хелий',
        mass: '4.003',
        category: 'noble-gas',
        period: 1,
        group: 18,
        description: 'Благороден газ, втори по разпространение елемент.',
        position: { row: 1, col: 18 }
    },
    // Добавете още елементи...
];
```

## 🔍 Как да тествате

### 1. Основна функционалност
- Отворете файла в браузър
- Проверете дали всички секции се виждат
- Натиснете F12 за Console
- Проверете за грешки

### 2. Рендериране на елементи
- Трябва да видите елементите в таблицата
- Всеки елемент трябва да показва: номер, символ, име, маса
- Проверете дали цветовете отговарят на категориите

### 3. Филтриране
- Тествайте всяка опция от dropdown-а
- Проверете дали показва само елементи от избраната категория
- Проверете дали броячът се обновява

### 4. Търсене
- Въведете име на елемент (напр. "Водород")
- Въведете символ (напр. "H")
- Проверете case-insensitive търсене (напр. "водород", "ВОДОРОД")

### 5. Детайли
- Кликнете на елемент
- Проверете дали всички полета се попълват
- Проверете дали бутонът "затвори" работи

### 6. Викторина
- Стартирайте викторината
- Отговорете на няколко въпроса
- Проверете дали точките се броят правилно
- Проверете feedback-а за правилни/грешни отговори

### 7. Responsive
- Отворете DevTools (F12)
- Toggle Device Toolbar (Ctrl+Shift+M)
- Тествайте на различни резолюции
- Проверете дали Grid layout се адаптира

## 🎯 Критерии за успех

Вашият проект е успешен ако:

- ✅ Показват се поне 25 елемента
- ✅ Филтрирането работи за всички категории
- ✅ Търсенето намира елементи по име и символ
- ✅ Кликът на елемент показва детайли
- ✅ Викторината генерира 10 въпроса
- ✅ Всички функции работят без грешки в Console
- ✅ Дизайнът е responsive
- ✅ Кодът е добре коментиран

## 💡 Допълнителни предизвикателства

Ако искате да отидете още по-далеч:

1. **Добавете повече елементи** - опитайте се да достигнете 50+ или дори 118
2. **Добавете още категории** - халогени, преходни метали, лантаноиди
3. **Подобрете викторината** - различни типове въпроси (отгатване на символ, маса, и т.н.)
4. **Добавете LocalStorage** - запазване на най-добър резултат
5. **Добавете звукови ефекти** - за правилни/грешни отговори
6. **Анимации при зареждане** - елементите да се появяват един по един
7. **Темна/светла тема** - бутон за превключване
8. **Експорт на резултати** - възможност за download на статистики

## 📞 Помощ

Ако имате въпроси или нужда от помощ:

1. Прочетете отново инструкциите в TODO коментарите
2. Проверете примерите в PRIMERI_Populvane файла
3. Погледнете Console за грешки (F12)
4. Попитайте екипния си лидер
5. Потърсете учителя

## 🎉 Успех!

Този проект е отлична възможност да комбинирате химията с програмирането. Фокусирайте се върху:

- **Масивите** - основата на проекта
- **For цикли** - за обхождане на елементите
- **Switch/If-else** - за филтриране и търсене
- **Event listeners** - за интерактивност

